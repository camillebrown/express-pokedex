<div id="fav-pokes">
    <h1 class="text-center"> The Pokedex</h1>
    <ul id="fav-pokes">
        <% favorites.forEach(fave=> { %>
            <li><a href='/pokemon/<%= fave.name %>'><%= fave.name %></a></li>
        <% }) %>
    </ul>
</div>